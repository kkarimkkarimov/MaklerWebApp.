@{
    ViewData["Title"] = "Home Page";
    var apiHealthy = ViewData["ApiHealthy"] as bool?;
    var publicListingCount = ViewData["PublicListingCount"] as int?;
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    @if (apiHealthy.HasValue)
    {
        <p>API status: <strong>@(apiHealthy.Value ? "Healthy" : "Unavailable")</strong></p>
    }
    @if (publicListingCount.HasValue)
    {
        <p>Public listings available: <strong>@publicListingCount.Value</strong></p>
    }
</div>
